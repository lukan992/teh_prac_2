# Environment

- Python: `>=3.11`
- Dependency management: `uv`
- Installation policy: use `uv add ...` for dependencies and `uv run ...` for commands
- Expected hardware: NVIDIA GeForce RTX 5070 Ti x2 with 16 GB VRAM each
- CPU fallback is supported for classical methods and smoke testing
